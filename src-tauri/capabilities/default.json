{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Boot OS Pro",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:path:default",
    "dialog:allow-open",
    "fs:allow-exists",
    "fs:allow-read-file",
    "shell:allow-open"
  ]
}
